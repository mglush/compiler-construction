classA {
     integer y;

     classA(integer val) -> none {
         y = val;
     }
}

classB {
    integer x;

    classB() -> none {
        x = 1;
    }

    fib(classA a) -> classA {

      print x;
      print x;
      print x;
      print x;
      print x;

	    tmp = x;

	    x = x + a.y;

	    print x;

	    return new classA(tmp);
    }

    dofib() -> none {      
      fib(fib(fib(fib(fib(fib(fib(fib(new classA(1)))))))));
    }

}


Main {

     main() -> none {
	    classB b;

	    b = new classB();
	    b.dofib();
	    
     }

}
